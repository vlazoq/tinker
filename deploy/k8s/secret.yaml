# IMPORTANT: Replace CHANGEME values before deploying. Consider using sealed-secrets or Vault.
apiVersion: v1
kind: Secret
metadata:
  name: tinker-secrets
  namespace: tinker
  labels:
    app.kubernetes.io/name: tinker
type: Opaque
stringData:
  TINKER_SECRET_KEY: "CHANGEME"
  TINKER_SLACK_WEBHOOK: "CHANGEME"
  TINKER_ALERT_WEBHOOK: "CHANGEME"
